import React from "react";
import { View, Text, Button, StyleSheet } from "react-native";

const CounterScreen = () => {
  return (
    <View>
      <Text>Counter Default:</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default CounterScreen;
